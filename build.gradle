plugins {
    id 'application'
}

application {
    mainModule.set('testModule')
    mainClass.set('testPackage.Main')
}

compileJava {
    modularClasspathHandling.getInferModulePath().set(true)
}

run {
    modularClasspathHandling.getInferModulePath().set(true)
}
